import { ConsoleLogger, LevelLogger } from './logger';

const LOG_SOURCE = 'TEST';
const LOG_CATEGORY = 'TEST';

describe('LevelLogger', () => {
  it('should call log function based on level', () => {});
});
