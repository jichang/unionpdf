{
  "name": "onepdf",
  "private": true,
  "devDependencies": {
    "husky": "^8.0.1",
    "lerna": "^5.4.0",
    "prettier": "^2.7.1"
  },
  "scripts": {
    "format": "prettier --write .",
    "build": "lerna run build",
    "check": "lerna run check",
    "test": "lerna run test",
    "prepare": "husky install"
  }
}
