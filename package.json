{
  "name": "onepdf",
  "private": true,
  "devDependencies": {
    "husky": "^8.0.1",
    "lerna": "^5.4.0",
    "prettier": "^2.7.1"
  },
  "scripts": {
    "format": "prettier --write .",
    "build": "lerna run build",
    "typecheck": "lerna run typecheck",
    "test": "lerna run test",
    "prepare": "husky install"
  }
}
